# Agent Rules

- ALWAYS run the frontend build command (e.g. `npm run build` or `npx ng build`) before considering a frontend task finished. This ensures that no compilation errors, syntax errors, or TypeScript type issues were introduced during the implementation. Never assume the code compiles just because it looks correct.
